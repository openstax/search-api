require 'rails_helper'

RSpec.describe "work_index_jobs", type: :rake do
  include_context "rake"

  xit "stubs things and tests them" do
    # found a bad keyword argument through manual testing on AWS... should stub
    # the code in the script to at least make sure the calls are right
    call
  end
end
